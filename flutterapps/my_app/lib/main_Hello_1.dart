
import 'package:flutter/material.dart';

void main(){
  //UI starting point
  var HelloWidget = Text(
      "Hello Flutter",
    textAlign: TextAlign.center,
    textDirection: TextDirection.ltr
  );
  runApp(HelloWidget);
}